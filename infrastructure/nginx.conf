events {}

http {
    server {
        listen 0.0.0.0:80;

        root /usr/share/nginx/html;

        location /api/ {

        }

        location = / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}